import Foundation
import UIKit

struct TNConstants {
    struct Auth {
        static let minimumPasswordLength = 6
        static let minimumTodoTitleLength = 3
    }
}

extension TNConstants {
    static let drawerWidth: CGFloat = UIScreen.main.bounds.width * 0.75
    // static let drawerWidth: CGFloat = 300
}
